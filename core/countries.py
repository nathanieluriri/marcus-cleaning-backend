from __future__ import annotations

# Keep the list explicit and easy to update as product geography changes.
ALLOWED_COUNTRIES: tuple[str, ...] = (
    "NG",
)

COUNTRY_CURRENCY_MAP: dict[str, str] = {
    "NG": "NGN",
}
